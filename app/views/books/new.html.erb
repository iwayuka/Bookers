<h2>New book</h2>
  <!-- form_with部分の追加 -->
  <%= form_with model:@book, local:true do |f| %>
    <label>Title</label>
    <%= f.text_field :title %>

    <label>Body</label>
    <%= f.text_field :body %>

    <%= f.submit 'Create Book' %>
  <% end %>